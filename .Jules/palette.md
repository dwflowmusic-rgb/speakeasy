## 2024-05-22 - Desktop Empty States
**Learning:** In PySide/Qt, `QStackedWidget` is the most robust pattern for toggling between a data view (List/Table) and an Empty State, ensuring layout stability compared to hiding/showing widgets manually.
**Action:** Use `QStackedWidget` by default when implementing empty states in desktop UI components.
